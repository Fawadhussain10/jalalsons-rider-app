// Deprecated: not imported anywhere. The active token lives in
// lib/config/app_config.dart (AppConfig.mapboxAccessToken), injected at build
// time via --dart-define=MAPBOX_TOKEN=pk.<your public token>.
// Never commit a real Mapbox token (public or secret) to this repo.
const String MAPBOX_ACCESS_TOKEN = 'pk.PLACEHOLDER_SET_MAPBOX_TOKEN_AT_BUILD_TIME';
